#include <cstdlib>
#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <sstream>
#include<math.h>
#include <stdio.h>
#include <stdlib.h>
#include "knn.h"
#include "distances.h"
#include "map"
#include"algorithm"



int main() {

//   vector<double> vect={1,2,2,1,5,55,2};
//   for (int i=0;i<vect.size();i++){
//       cout<<vect.at(i)<<endl;


//   }
//   cout<<MFA(vect)<<"----"<<endl;

// vector<vector<double>> data=Store_data("test.txt");
// for (double j=0; j< data.size(); j++)
// {
//     vector<double> tmp=data.at(j);
//     for(double k=0; k<tmp.size(); k++)
//         cout<<tmp.at(k)<<" | ";

//     cout<<"------------"<<endl;
// }
// map <vector<double>, double> MapOfDocs=BuildMap(data);
// for (map<vector<double>,double>::iterator it=MapOfDocs.begin(); it!=MapOfDocs.end(); ++it)
// {
//     cout << " => " << it->second << '\n';
//     vector<double> tmp1=it->first;
//     for(double k=0; k<tmp1.size(); k++)
//     cout<<tmp1.at(k)<<" | ";

//     cout<<"------------"<<endl;
//}
  // return 0;
}
